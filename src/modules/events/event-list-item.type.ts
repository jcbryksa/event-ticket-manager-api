export type EventListItem = {
  eventId: string
  status: string
  presentationTitle: string
  performerName: string
  locationName: string
  startDateTime: Date
  minPrice: number
  maxPrice: number
}
